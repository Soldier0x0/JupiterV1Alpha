# Example environment for native (no-docker) run
# Copy to native/.env and adjust values

APP_ENVIRONMENT=production
CORS_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:3000

# Security
JWT_SECRET_KEY=change_me

# Email (optional)
EMAIL_HOST=smtp.outlook.com
EMAIL_PORT=587
EMAIL_USER=harsha@projectjupiter.in
EMAIL_PASSWORD=
EMAIL_USE_TLS=true

# Super Admin bootstrap (used by backend/main.py on startup)
SUPER_ADMIN_EMAIL=admin@projectjupiter.in
SUPER_ADMIN_PASSWORD=ChangeMe123!
SUPER_ADMIN_TENANT_NAME=MainTenant

# Data paths
DUCKDB_PATH=data/jupiter_siem.db
